# Label-blind pilot fixture correction (superseded by PA-01)

Date: 2026-08-13 (Asia/Calcutta)

The first successful diagnostic extraction showed that the original 1,900-character carbon-chain fixture compressed to 275 MAMMAL tokens. Its rationale said it was near the configured 2,100-token limit, so the fixture did not satisfy the frozen technical composition contract.

Before any repeatability acceptance run and without inspecting any DILI outcome or predictive-performance result, `PILOT-20` was replaced by a valid isotope-explicit carbon chain. The first measurement, approximately 2,030 tokens, used the subsequently withdrawn prompt and is not acceptance evidence. Under the PA-01 checkpoint-compatible prompt, the same structure has 1,997 attended tokens, below but near the locked rejection threshold, with no unknown-token IDs or warnings. The other 19 structures and all outcome-related settings are unchanged.

The earlier vector file is diagnostic and withdrawn. The three accepted fresh-process runs required by PA-01 will use only the tracked corrected 20-row file. This record also requires per-molecule token counts, truncation status, special-token layout, unknown-token counts and tokenizer warnings in every extraction manifest.
